# DESIGN TOKENS — replace before any UI code is written.

> Run `python agents/build-design.py --build <id>` to generate this file.
> The generator researches design precedent for the specific product type,
> then outputs exact hex values, typography, spacing, named surface precedents,
> and anti-patterns.
>
> Copy the generated globals.css into app/globals.css before writing any components.

This placeholder must be replaced. No UI code should be written until DESIGN.md
contains real token values.
